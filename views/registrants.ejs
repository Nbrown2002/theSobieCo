<!DOCTYPE html> 
<html> 
<head>
    <link rel="stylesheet" href="style.css">
    <meta charset="UTF-8">
    <title> Registrants Page (Admin Exclusive) </title>
<body> 
    <main> 
    <h1> Registrants Page (Admin Exclusive) </h1> 
    <br> 
    <br> 
    <h2> Registrant List </h2>
    < %profileData.forEach(result=> { %>
        <div class="col-md-8">
  
  
          <form method="POST" action="update/">
            <input name="updateTitle" value="<%=result.Registrant_Name %>">
            <input name="updatePost" value="<%=result.Registrant_Status %>">
            <input type="hidden" name="updateId" value="<%=result._id %>">
            <button class="btn btn-primary" role="submit"><i class="bi bi-pencil"></i></button>
          </form>
  
  
  
          <form method="POST" action="delete/">
            <input type="hidden" name="deleteId" value="<%=result._id %>">
            <button class="btn btn-primary" role="submit"><i class="bi bi-trash3"></i></button>
          </form>
  
  
        </div>
        <% }); %>
    <a href="/admin" style="color:white">Return to Admin Page</a>    
    </main>
</body>    
</head>
</html>